#!/usr/bin/node

// do the same as task 3 but with array and loop

let myarr = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < myarr.length; i++) {
  console.log(myarr[i]);
}
